# 高质量视频生成工业级方案

> 项目背景：当前系统已经完成内容生产闭环，但视频生成链仍处于 MVP 验证阶段。  
> 当前问题：`script -> TTS -> SRT -> ffmpeg 纯色背景 -> final_video.mp4` 可以跑通，但质量上限低。  
> 目标：把视频层升级为「分镜 + 素材 + 模板 + 动效 + 字幕对齐 + 音频后期 + 视觉质检」的工业级生产流水线。

---

## 1. 总体判断

当前视频链路适合验证闭环，不适合作为最终生产链。

现状大概是：

```text
chinese_script.md
→ 提取 # 口播稿
→ TTS
→ 句子切分
→ 估算字幕时间
→ 生成 SRT
→ ffmpeg 纯色背景 + 字幕 + 音频
→ final_video.mp4
```

这条链路的问题：

```text
1. 背景单一，像测试视频
2. 字幕时间靠估算，不够精准
3. 没有分镜
4. 没有素材调度
5. 没有封面
6. 没有平台差异化模板
7. 没有动效体系
8. 没有视觉质量检测
9. fallback 静音只适合测试，不适合生产
10. ffmpeg filter 不适合长期承载复杂视觉系统
```

最终方案应该是：

```text
Python 主系统：内容理解、脚本、分镜、素材规划、调度
Remotion：视频画面、动效、模板、封面、多比例渲染
FFmpeg：音频后期、响度标准化、转码、压缩、兜底
WhisperX / stable-ts：字幕精准对齐
视觉 QC：发布前质量检测
```

---

## 2. 目标架构

### 2.1 新视频生成链路

```text
chinese_script.md
        ↓
platform_script.md
        ↓
storyboard.json
        ↓
asset_plan.json
        ↓
voice.mp3
        ↓
word_timestamps.json
        ↓
subtitle_plan.json / subtitles.ass
        ↓
render_manifest.json
        ↓
Remotion render
        ↓
ffmpeg post-process
        ↓
visual_quality_report.json
        ↓
final_video.mp4
        ↓
cover.png
        ↓
platform_publish_package.json
```

### 2.2 不再直接从脚本生成视频

不要这样：

```text
script -> final_video
```

要这样：

```text
script -> storyboard -> asset plan -> render manifest -> final_video
```

这是高质量视频和普通自动拼接视频的分水岭。

---

## 3. 新增目录结构

建议在现有 `content_asset_mvp` 下新增：

```text
content_asset_mvp/
├── app/
│   ├── storyboard_builder.py
│   ├── asset_planner.py
│   ├── render_manifest.py
│   ├── cover_generator.py
│   ├── audio_mastering.py
│   ├── subtitle_engine.py
│   ├── tts_engine.py
│   ├── ffmpeg_renderer.py
│   ├── remotion_renderer.py
│   └── visual_qc.py
├── video_engine/
│   └── remotion/
│       ├── package.json
│       ├── tsconfig.json
│       ├── remotion.config.ts
│       ├── src/
│       │   ├── Root.tsx
│       │   ├── render.ts
│       │   ├── compositions/
│       │   │   ├── DouyinExplainer.tsx
│       │   │   ├── KuaishouExplainer.tsx
│       │   │   ├── WechatChannelsExplainer.tsx
│       │   │   └── BilibiliExplainer.tsx
│       │   ├── components/
│       │   │   ├── BigClaimCard.tsx
│       │   │   ├── DataCard.tsx
│       │   │   ├── ScreenshotFrame.tsx
│       │   │   ├── FlowDiagram.tsx
│       │   │   ├── SubtitleLayer.tsx
│       │   │   ├── ProgressBar.tsx
│       │   │   ├── ChapterTitle.tsx
│       │   │   ├── RiskCard.tsx
│       │   │   └── Cover.tsx
│       │   └── styles/
│       │       ├── theme.ts
│       │       └── typography.ts
│       └── public/
│           ├── fonts/
│           ├── backgrounds/
│           ├── icons/
│           └── sound_effects/
└── assets/
    ├── backgrounds/
    ├── icons/
    ├── broll/
    ├── bgm/
    ├── fonts/
    └── templates/
```

---

## 4. 输出产物结构

每个内容包输出：

```text
output/<content_id>/
├── core/
│   ├── analysis.json
│   ├── score.json
│   ├── risk_report.json
│   └── hook_points.json
├── douyin/
│   ├── script.md
│   ├── storyboard.json
│   ├── asset_plan.json
│   ├── voice.mp3
│   ├── word_timestamps.json
│   ├── subtitles.ass
│   ├── render_manifest.json
│   ├── cover.png
│   ├── final_video.mp4
│   └── visual_quality_report.json
├── kuaishou/
│   ├── script.md
│   ├── storyboard.json
│   ├── asset_plan.json
│   ├── cover.png
│   ├── final_video.mp4
│   └── visual_quality_report.json
├── wechat_channels/
│   ├── script.md
│   ├── storyboard.json
│   ├── asset_plan.json
│   ├── cover.png
│   ├── final_video.mp4
│   └── visual_quality_report.json
└── bilibili/
    ├── script.md
    ├── storyboard.json
    ├── asset_plan.json
    ├── cover.png
    ├── final_video.mp4
    └── visual_quality_report.json
```

---

## 5. Storyboard 层

### 5.1 作用

`storyboard.json` 是视频的结构化分镜。

它解决的问题：

```text
1. 每段话对应什么画面
2. 每个镜头持续多久
3. 用什么视觉类型
4. 屏幕上显示什么文字
5. 需要什么素材
6. 用什么动效
7. 哪些地方有风险提醒
```

### 5.2 新增文件

```text
app/storyboard_builder.py
```

### 5.3 输入

```text
chinese_script.md
analysis.json
score.json
risk_report.json
platform
```

### 5.4 输出示例

```json
{
  "content_id": "yt_xxx",
  "platform": "douyin",
  "aspect_ratio": "9:16",
  "width": 1080,
  "height": 1920,
  "fps": 30,
  "style": "tech_business_explainer",
  "duration_target": 90,
  "scenes": [
    {
      "scene_id": "hook",
      "order": 1,
      "duration": 6,
      "voiceover": "这个老外一个人做了个小工具，居然月入几万美金。",
      "visual_type": "big_claim_card",
      "on_screen_text": "一个人 / 小工具 / 月入几万美金",
      "motion": "fast_zoom_in",
      "assets_needed": [],
      "risk_flags": ["income_claim_needs_check"]
    },
    {
      "scene_id": "breakdown_1",
      "order": 2,
      "duration": 18,
      "voiceover": "重点不是他技术多牛，而是他抓住了一个很细的需求。",
      "visual_type": "process_card",
      "on_screen_text": "细分需求 → 工具产品 → 平台传播",
      "motion": "card_slide",
      "assets_needed": ["website_screenshot"]
    }
  ]
}
```

### 5.5 分镜类型

第一批只做这些：

```text
big_claim_card              大字爆点卡
project_intro_card          项目介绍卡
screenshot_breakdown        网页/产品/GitHub 截图拆解
data_card                   数据卡
flow_diagram                流程图
comparison_card             对比卡
risk_warning_card           风险提示卡
quote_card                  金句卡
chapter_title               章节标题
summary_card                总结卡
comment_prompt_card         评论引导卡
```

---

## 6. Asset Planner 素材规划层

### 6.1 新增文件

```text
app/asset_planner.py
```

### 6.2 作用

根据 `storyboard.json` 自动决定每个场景需要哪些素材。

素材类型：

```text
1. 网页截图
2. GitHub 仓库截图
3. README 截图
4. 产品 logo
5. 图标
6. 数据卡片
7. 流程图
8. 背景视频
9. 背景图片
10. BGM
11. 音效
12. 封面主体图
```

### 6.3 输出示例

```json
{
  "content_id": "yt_xxx",
  "platform": "douyin",
  "assets": [
    {
      "asset_id": "github_homepage",
      "type": "screenshot",
      "source": "github_meta.html_url",
      "path": "assets/github_homepage.png",
      "usage": ["scene_2", "scene_3"],
      "status": "ready"
    },
    {
      "asset_id": "growth_chart",
      "type": "generated_chart",
      "path": "assets/growth_chart.png",
      "usage": ["scene_4"],
      "status": "ready"
    }
  ]
}
```

### 6.4 素材优先级

```text
第一优先级：真实截图、真实项目页面、真实 GitHub 页面
第二优先级：系统生成的信息卡片、流程图、数据图
第三优先级：素材库背景、图标、B-roll
第四优先级：AI 生成背景或抽象画面
```

不要用 AI 生成画面表达事实。  
事实类画面必须真实、可核查。

---

## 7. Remotion 渲染层

### 7.1 为什么用 Remotion

FFmpeg 适合转码、合并、压缩，不适合维护复杂视觉设计。

Remotion 适合：

```text
1. React 组件化视频
2. 动态卡片
3. 统一视觉风格
4. 模板复用
5. 多平台比例
6. 封面生成
7. 复杂字幕动效
8. 批量渲染
```

### 7.2 调用方式

Python 主系统生成：

```text
render_manifest.json
```

然后调用：

```bash
node video_engine/remotion/render.js output/<content_id>/<platform>/render_manifest.json
```

### 7.3 render_manifest 示例

```json
{
  "content_id": "yt_xxx",
  "platform": "douyin",
  "composition": "DouyinExplainer",
  "width": 1080,
  "height": 1920,
  "fps": 30,
  "audio_path": "voice.mp3",
  "cover_output": "cover.png",
  "video_output": "final_video.mp4",
  "theme": "dark_tech",
  "scenes": [
    {
      "type": "BigClaimCard",
      "start_frame": 0,
      "duration_frames": 180,
      "title": "一个人做小工具",
      "subtitle": "月入几万美金？",
      "motion": "zoom",
      "background": "dark_grid"
    },
    {
      "type": "ScreenshotFrame",
      "start_frame": 180,
      "duration_frames": 420,
      "title": "重点不是技术",
      "subtitle": "而是抓住细分需求",
      "asset_path": "assets/website_screenshot.png",
      "motion": "pan_highlight"
    }
  ],
  "subtitles": {
    "type": "component",
    "path": "subtitle_plan.json"
  }
}
```

---

## 8. 第一批 Remotion 组件

### 8.1 BigClaimCard

用途：开头 3 秒抓人。

效果：

```text
大字标题
强对比背景
轻微 zoom in
关键词逐个弹出
进度条开始
```

字段：

```json
{
  "title": "一个人做小工具",
  "subtitle": "月入几万美金？",
  "keywords": ["一个人", "小工具", "几万美金"]
}
```

### 8.2 ScreenshotFrame

用途：展示网页、GitHub、产品页面。

效果：

```text
浏览器窗口外框
截图轻微平移
重点区域高亮框
角标说明
```

字段：

```json
{
  "asset_path": "assets/github_homepage.png",
  "highlight_boxes": [
    {"x": 120, "y": 300, "w": 500, "h": 120, "label": "Stars"}
  ]
}
```

### 8.3 DataCard

用途：展示数据和结论。

效果：

```text
数字滚动
卡片弹出
指标对比
风险标记
```

字段：

```json
{
  "label": "月收入",
  "value": "$30K",
  "note": "需核查来源"
}
```

### 8.4 FlowDiagram

用途：展示商业模式、增长路径、项目流程。

效果：

```text
节点逐个出现
箭头连线
关键词高亮
```

字段：

```json
{
  "nodes": ["细分需求", "工具产品", "内容传播", "付费转化"],
  "edges": [[0,1], [1,2], [2,3]]
}
```

### 8.5 SubtitleLayer

用途：高质量字幕。

要求：

```text
每屏 8-16 个中文字
最多 2 行
关键词高亮
不挡平台按钮
抖音/快手字号更大
B站字号更小但信息密度更高
```

### 8.6 Cover

用途：自动生成封面。

字段：

```json
{
  "main_title": "一个人做小工具",
  "sub_title": "为什么能月入几万美金？",
  "visual": "website_screenshot",
  "platform": "douyin"
}
```

---

## 9. 平台模板设计

### 9.1 抖音模板

目标：

```text
强钩子
强节奏
前 3 秒抓人
高完播
评论诱导
```

参数：

```text
比例：9:16
分辨率：1080x1920
时长：30-90 秒
字幕：大字、短句
节奏：每 3-6 秒有画面变化
```

结构：

```text
0-3 秒：爆点大字
3-12 秒：结果或反差
12-45 秒：拆 2-3 个原因
45-75 秒：给判断
75-90 秒：评论引导
```

视觉：

```text
大字卡片
快速切换
关键词高亮
背景动效
进度条
```

### 9.2 快手模板

目标：

```text
接地气
讲人话
强调普通人能不能学
评论互动
```

参数：

```text
比例：9:16
分辨率：1080x1920
时长：30-120 秒
字幕：大字，语气更口语
节奏：稍慢于抖音
```

结构：

```text
这个项目是啥
它怎么赚钱
普通人能不能做
坑在哪里
一句结论
```

### 9.3 微信视频号模板

目标：

```text
稳重
观点感
适合转发
适合泛人群
```

参数：

```text
比例：9:16
时长：1-3 分钟
字幕：清晰稳重
画面：少花哨，多观点卡
```

结构：

```text
现象
背后逻辑
普通人启发
风险
结论
```

### 9.4 B站模板

目标：

```text
信息密度
收藏价值
逻辑完整
可信度
```

参数：

```text
比例：16:9
分辨率：1920x1080
时长：3-8 分钟
字幕：可稍小
章节：必须有
```

结构：

```text
项目是什么
为什么火
怎么赚钱/增长
技术或产品结构
国内能不能复制
风险
结论
```

视觉：

```text
网页截图
GitHub 截图
数据卡片
流程图
对比表
章节标题
```

---

## 10. 字幕系统升级

### 10.1 当前问题

当前字幕时间按句子长度估算，适合测试，不适合发布。

问题：

```text
1. 音频和字幕容易错位
2. 无法逐词高亮
3. 句子可能过长
4. 平台安全区不可控
5. 字幕样式不够统一
```

### 10.2 推荐链路

```text
TTS 生成音频
→ WhisperX / stable-ts 重新识别
→ 得到 word_timestamps.json
→ subtitle_engine.py 生成 subtitle_plan.json
→ Remotion SubtitleLayer 渲染
```

### 10.3 subtitle_plan 示例

```json
{
  "segments": [
    {
      "start": 0.0,
      "end": 2.1,
      "text": "这个老外一个人做了个小工具",
      "highlight_words": ["一个人", "小工具"]
    },
    {
      "start": 2.1,
      "end": 4.2,
      "text": "居然月入几万美金",
      "highlight_words": ["几万美金"]
    }
  ]
}
```

### 10.4 字幕规范

```text
抖音/快手：
- 每屏 8-14 个中文字
- 字号大
- 关键词高亮
- 底部留安全区

视频号：
- 每屏 10-16 个中文字
- 字幕稳重
- 不要太花

B站：
- 每屏 12-24 个中文字
- 可放双行
- 章节标题更重要
```

---

## 11. 音频系统升级

### 11.1 TTS 方案

生产级不要用静音 fallback，也不要长期依赖 Edge-TTS。

推荐：

```text
中文生产：火山引擎 TTS / Azure Neural Voice
备用：OpenAI TTS
开发测试：Edge-TTS
兜底：静音，只允许 mock 使用
```

### 11.2 新增文件

```text
app/tts_engine.py
app/audio_mastering.py
```

### 11.3 TTS 功能要求

```text
1. 分段合成
2. 失败重试
3. 语速控制
4. 停顿控制
5. 数字读法修正
6. 英文词读法修正
7. 情绪强度
8. 音色配置
9. 输出 mp3/wav
```

### 11.4 音频后期

必须做：

```text
1. loudnorm 响度标准化
2. 去开头/结尾空白
3. 背景音乐混音
4. 音效点缀
5. 防止爆音
```

示例：

```bash
ffmpeg -i voice.mp3 -af loudnorm=I=-14:TP=-1.5:LRA=11 voice_mastered.mp3
```

短视频可以略响，但不要爆音。

---

## 12. 封面生成

### 12.1 新增文件

```text
app/cover_generator.py
```

### 12.2 每个平台生成不同封面

```text
douyin_cover.png
kuaishou_cover.png
wechat_cover.png
bilibili_cover.png
```

### 12.3 封面结构

```text
主标题：8-14 字
副标题：10-20 字
主体：网页截图 / GitHub 截图 / 工具 Logo / 数据卡
背景：强对比
平台安全区：预留
```

### 12.4 平台差异

```text
抖音：大字、冲突、反差
快手：直接、接地气
视频号：稳重、观点感
B站：信息清楚、可信度、项目名明显
```

---

## 13. 视觉质量检测

### 13.1 新增文件

```text
app/visual_qc.py
```

### 13.2 检测项

```text
1. 是否黑屏
2. 是否静音
3. 音量是否过低
4. 是否爆音
5. 字幕是否超出安全区
6. 字幕是否过长
7. 每个画面停留是否过久
8. 是否缺少封面
9. 是否缺少真实素材
10. 是否用了原视频画面
11. 视频码率是否过低
12. 分辨率是否符合平台
13. 是否生成平台对应比例
14. 是否存在 fallback placeholder
15. 是否存在未核查收入/数据 claims
```

### 13.3 输出示例

```json
{
  "pass": false,
  "score": 76,
  "issues": [
    "第 2 个场景停留 28 秒，画面变化不足",
    "字幕平均每屏 22 个中文字，过长",
    "音频未做 loudnorm"
  ],
  "must_fix": [
    "增加场景切换",
    "缩短字幕",
    "执行音频标准化"
  ],
  "warnings": [
    "当前视频没有真实网页截图，可信度较弱"
  ]
}
```

---

## 14. 视频质量评分标准

### 14.1 自动评分维度

```text
画面丰富度：20%
字幕质量：20%
声音质量：20%
节奏：15%
素材可信度：15%
平台适配：10%
```

### 14.2 发布门槛

```text
score >= 95：可直接进入发布队列
score 80-94：需要人工检查
score < 80：不建议发布
```

> **对齐状态**：当前 `build_video_quality_report` 公式的数学上限是 93.8
> （subtitle / voice / hook_strength 三项硬顶分别 86 / 92 / 86，合计占
> 54% 权重）。在公式里拆掉这三项硬顶、并把 WhisperX word-level / 音频
> loudnorm / 真实素材替换 / 硬性失败条件判定（§14.3）落地之前，95
> 这个门槛**不可达**，整条流水线会卡在人工检查档位。这是刻意设定的
> 高压线，是"先定目标、再反推能力"的节奏，不是让当前公式就能命中的
> KPI。

### 14.3 硬性失败条件

```text
1. 无声音
2. 黑屏超过 2 秒
3. 字幕缺失
4. 视频长度为 0
5. final_video.mp4 文件损坏
6. 仍使用 silence fallback
7. 有未核查高风险收益承诺
8. 使用原视频画面作为主要画面
```

---

## 15. 分阶段落地计划

## v0.2：从“能生成”到“能发布”

先不接 Remotion，先把现有 ffmpeg 版升级到能发。

### 目标

```text
视频不再是纯色背景
字幕明显好看
配音可听
有封面
有基础质检
```

### 任务

```text
1. 静音 fallback 只允许 mock 使用
2. 接稳定中文 TTS
3. 加 loudnorm
4. 用 ASS 字幕替代普通 SRT
5. 加封面生成
6. 每 8-12 秒自动换一个信息卡片画面
7. 加 visual_qc.py
```

### 验收标准

```text
10 条真实视频里：
- 8 条以上能生成 final_video.mp4
- 无静音视频
- 字幕不明显错位
- 每条都有封面
- visual_qc 能输出质量分
```

---

## v0.3：接 Remotion，进入专业模板阶段

### 目标

```text
视频有统一视觉风格
能生成平台化模板
画面有动效
封面可控
```

### 任务

```text
1. 新增 storyboard_builder.py
2. 新增 asset_planner.py
3. 新增 render_manifest.py
4. 新增 video_engine/remotion/
5. 做 3 个模板：
   - 抖音/快手爆点模板
   - 视频号观点模板
   - B站项目拆解模板
6. Python 调 Node/Remotion 渲染
7. Remotion 输出 final_video.mp4 和 cover.png
```

### 验收标准

```text
同一内容可以生成：
- douyin/final_video.mp4
- kuaishou/final_video.mp4
- wechat_channels/final_video.mp4
- bilibili/final_video.mp4

每个平台画面比例、标题、字幕、封面不同。
```

---

## v0.4：字幕对齐和素材系统

### 目标

```text
字幕精准
素材可信
画面更接近人工剪辑
```

### 任务

```text
1. 接 WhisperX / stable-ts
2. 生成 word_timestamps.json
3. 生成 subtitle_plan.json
4. 自动截图网页 / GitHub / 产品页
5. 资产库标签系统
6. 每个 scene 自动匹配素材
7. 视觉 QC 加强
```

### 验收标准

```text
1. 字幕和语音基本同步
2. 关键词高亮准确
3. 每条视频至少 3 类真实素材
4. 每 8-12 秒有画面变化
5. visual_qc >= 95 的视频才进入发布队列
```

---

## v0.5：工业化批量生产

### 目标

```text
批量视频生成稳定
质量可控
可人工微调
```

### 任务

```text
1. 批量渲染队列
2. 渲染失败重试
3. 人工审核页面预览 storyboard
4. 可修改 scene 文案和素材
5. 可重渲染单个平台
6. 质量报告进入发布看板
7. 平台数据反馈反哺模板选择
```

---

## 16. 开源项目借鉴

### 16.1 Remotion

定位：主渲染引擎。

用途：

```text
视频模板
动态字幕
封面生成
多比例渲染
批量渲染
React 组件化动效
```

建议：直接作为主视频引擎，不要只借鉴。

### 16.2 MoviePy

定位：轻量 fallback。

用途：

```text
简单拼接
本地预览
MVP 兜底
测试环境
```

建议：保留，但不要作为高质量主引擎。

### 16.3 MoneyPrinterTurbo / NarratoAI

定位：自动视频流水线参考。

可借鉴：

```text
脚本生成
素材检索
TTS
字幕
视频合成
配置管理
```

注意：不要照搬成低质批量视频系统。

### 16.4 WhisperX / stable-ts

定位：字幕对齐。

用途：

```text
词级时间戳
句级字幕对齐
关键词高亮
音频和字幕同步
```

建议：v0.4 接入。

### 16.5 Manim

定位：解释动画组件。

适合：

```text
流程图
增长曲线
商业模式图
系统架构图
数据变化动画
```

建议：不要全片用 Manim，只在解释复杂逻辑时插入。

---

## 17. 你当前代码的具体改法

### 17.1 当前 `media_producer.py` 拆分

现在 `media_producer.py` 既做 TTS、字幕、翻译、渲染、fallback，职责过重。

建议拆成：

```text
media_producer.py          # 只做调度
tts_engine.py              # 配音
subtitle_engine.py         # 字幕
audio_mastering.py         # 音频后期
ffmpeg_renderer.py         # 旧渲染兜底
remotion_renderer.py       # 新渲染入口
visual_qc.py               # 视觉质检
```

### 17.2 `prepare_media_job()` 状态修正

不要再写：

```text
status = skipped
issues = ["MVP v1 does not generate final video assets."]
```

建议改成：

```text
status = ready_to_render
```

或者拆两个文件：

```text
media_job_pending.json
media_job_result.json
```

### 17.3 新命令设计

生成 storyboard：

```bash
python -m app.main --build-storyboard "<content_id>" --platform douyin
```

生成素材计划：

```bash
python -m app.main --plan-assets "<content_id>" --platform douyin
```

生成 Remotion manifest：

```bash
python -m app.main --build-render-manifest "<content_id>" --platform douyin
```

渲染单个平台：

```bash
python -m app.main --render-platform-video "<content_id>" --platform douyin
```

批量渲染所有平台：

```bash
python -m app.main --render-platform-videos-all "<content_id>"
```

视觉质检：

```bash
python -m app.main --visual-qc "<content_id>" --platform douyin
```

---

## 18. 高质量视频的最终闭环

最终闭环应该是：

```text
内容源
→ analysis.json
→ score.json
→ risk_report.json
→ platform_script.md
→ storyboard.json
→ asset_plan.json
→ voice.mp3
→ word_timestamps.json
→ subtitle_plan.json
→ render_manifest.json
→ Remotion render
→ ffmpeg loudnorm/post-process
→ cover.png
→ visual_quality_report.json
→ platform_publish_package.json
→ publish_tasks.json
→ metrics
→ feedback_report.json
→ 模板/选题/平台权重调整
```

---

## 19. 最短执行路线

不要一口气做完。按下面执行：

### 第 1 步：修现有视频链

```text
1. 禁止生产模式静音 fallback
2. 接稳定中文 TTS
3. 加 loudnorm
4. 加封面
5. 加 ASS 字幕
```

### 第 2 步：加 storyboard

```text
1. storyboard_builder.py
2. 根据平台生成不同 storyboard
3. 每 8-12 秒一个 scene
```

### 第 3 步：加 Remotion

```text
1. 新建 video_engine/remotion
2. 做 BigClaimCard / DataCard / SubtitleLayer
3. Python 调 Node 渲染
```

### 第 4 步：多平台模板

```text
1. DouyinExplainer
2. KuaishouExplainer
3. WechatChannelsExplainer
4. BilibiliExplainer
```

### 第 5 步：质检和反馈

```text
1. visual_qc.py
2. 质量分低于 80 不发布（≥95 自动发布，80-94 人工检查）
3. 数据表现反哺模板选择
```

---

## 20. 结论

高质量视频不是单靠 AI 文生视频，也不是堆 ffmpeg filter。

正确路线是：

```text
分镜结构化
+ 真实素材
+ 模板化动效
+ 精准字幕
+ 专业配音
+ 音频后期
+ 封面生成
+ 视觉质检
+ 平台适配
```

最适合当前项目的技术组合：

```text
Python 主系统
+ Remotion 视频引擎
+ FFmpeg 后处理
+ WhisperX / stable-ts 字幕对齐
+ 火山/ Azure / OpenAI TTS
+ Playwright 截图
```

最终目标：

```text
不是自动生成一个视频，
而是自动生成一套平台适配、视觉统一、节奏清晰、可质检、可复盘的视频内容产品。
```
