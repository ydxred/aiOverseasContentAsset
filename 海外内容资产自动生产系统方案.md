# 海外内容资产自动生产系统方案

> 版本：v1.0  
> 目标：把 YouTube / 海外内容自动转化为中文内容资产  
> 模式：全自动生产，人工可选审核，默认不自动发布

---

## 0. 项目定位

### 0.1 不做什么

不做低级搬运：

```text
下载 YouTube 视频
→ 翻译
→ 换配音
→ 原画面重发
```

这种方式风险高，账号容易废，长期价值低。

### 0.2 要做什么

做自动化内容再加工系统：

```text
国外信息源
→ 自动采集
→ 自动转写
→ 自动理解
→ 自动评分
→ 自动重构中文内容
→ 自动生成视频 / 图文 / SEO 文章 / 标题 / 简介 / 标签
→ 自动入库
→ 自动进入待发布队列
```

项目本质不是“YouTube 搬运系统”，而是：

> 海外内容机会发现 + 中文内容资产自动生产系统

---

## 1. 总体架构

### 1.1 总链路

```text
频道/关键词监控
        ↓
视频发现与去重
        ↓
基础信息抓取
        ↓
字幕/音频获取
        ↓
ASR 转写
        ↓
内容清洗
        ↓
LLM 内容理解
        ↓
选题评分
        ↓
风险检测
        ↓
中文脚本重构
        ↓
标题/封面文案/简介/标签生成
        ↓
TTS 配音
        ↓
字幕生成
        ↓
视频合成
        ↓
质量检测
        ↓
入库归档
        ↓
待发布队列
        ↓
人工审核 / 自动发布
```

---

## 2. 系统模块拆分

### 2.1 Source Monitor：源监控模块

#### 作用

自动发现值得处理的视频和内容源。

#### 输入

```text
YouTube 频道列表
YouTube 搜索关键词
RSS 源
Twitter/X 链接
Reddit 链接
独立博客链接
国外工具站
```

#### 输出

```json
{
  "source": "youtube",
  "url": "https://youtube.com/watch?v=xxx",
  "channel": "xxx",
  "title": "xxx",
  "published_at": "2026-05-01",
  "duration": 623,
  "view_count": 100000,
  "language": "en",
  "status": "discovered"
}
```

#### 技术选择

| 任务 | 推荐工具 |
|---|---|
| YouTube 元数据 | `yt-dlp` |
| RSS | `feedparser` |
| 网页抓取 | `requests` / `playwright` |
| 去重 | SQLite / PostgreSQL |
| 定时任务 | cron / APScheduler |

#### 推荐 LLM

| 场景 | 推荐模型 |
|---|---|
| 标题粗分类 | GPT-5 mini / Gemini Flash / Qwen Turbo |
| 判断是否相关 | DeepSeek V4 Flash / GPT-5 mini |
| 不用 LLM | 规则 + 关键词 + 频道白名单 |

---

### 2.2 Sourcing Filter：初筛模块

#### 作用

判断视频是否值得进入生产链。

#### 评分维度

```text
1. 内容是否和账号定位相关
2. 国内是否稀缺
3. 是否有商业价值
4. 是否适合短视频
5. 是否适合长文 SEO
6. 是否有版权风险
7. 是否有事实核查风险
8. 是否容易被平台判搬运
```

#### 输出

```json
{
  "worth_processing": true,
  "score": 82,
  "reason": "海外独立开发案例，国内讨论较少，适合做中文拆解。",
  "best_format": ["short_video", "seo_article"],
  "risk_level": "medium"
}
```

#### 推荐 LLM

| 优先级 | 模型 | 用法 |
|---|---|---|
| 首选 | Gemini Pro | 长上下文、便宜、适合批量分析 |
| 次选 | GPT-5.4 / GPT-5.2 | 稳定，结构化输出好 |
| 国内备选 | Qwen Max / DeepSeek Pro | 中文判断、低成本 |
| 低成本批量 | Gemini Flash / DeepSeek Flash / GPT-5 mini | 大量视频粗筛 |

---

### 2.3 Downloader：下载模块

#### 作用

下载音频、字幕、缩略图和元数据。

#### 输出文件

```text
workspace/video_id/
├── meta.json
├── source_audio.mp3
├── original_subtitle.vtt
├── thumbnail.jpg
└── source_info.json
```

#### 工具

```text
yt-dlp
ffmpeg
```

#### 原则

可以下载原视频画面用于内部分析，但最终发布不建议直接使用原视频画面。发布内容优先使用：

```text
自制画面
截图讲解
PPT 风格
AI 图
图表
屏幕录制
素材库视频
```

---

### 2.4 ASR：语音转写模块

#### 作用

把音频转成带时间戳的文字。

#### 输出

```json
{
  "language": "en",
  "segments": [
    {
      "start": 0.0,
      "end": 4.8,
      "text": "Today I want to talk about..."
    }
  ]
}
```

#### 推荐方案

| 场景 | 推荐 |
|---|---|
| 稳定省事 | OpenAI Whisper API |
| 本地省钱 | faster-whisper |
| 大批量低成本 | 本地 GPU + faster-whisper |
| 中文/英文混合 | Whisper API / Gemini 多模态 |
| 需要高质量时间戳 | Whisper API + forced alignment |

#### 第一版建议

```text
第一版：OpenAI Whisper API
第二版：faster-whisper 本地化
```

第一版先跑通链路，不要为了省几毛钱把工程复杂度拉满。

---

### 2.5 Transcript Cleaner：转写清洗模块

#### 作用

清理 ASR 噪声。

#### 处理内容

```text
去除口头禅
去除重复句
修复明显断句
合并短句
保留时间戳
标记不确定段落
```

#### 推荐 LLM

| 场景 | 推荐 |
|---|---|
| 便宜批量清洗 | DeepSeek Flash / Gemini Flash / GPT-5 mini |
| 复杂长视频清洗 | Gemini Pro |
| 高质量中文整理 | Claude Sonnet / GPT-5.4 |

---

## 3. LLM 自动链设计

核心原则：

> 不要一个模型干所有事。不同环节用不同模型。贵模型只用在关键节点。

### 3.1 模型分工总表

| 链路 | 任务 | 首选模型 | 备选模型 | 是否需要强模型 |
|---|---|---|---|---|
| 初筛 | 标题/简介判断 | Gemini Flash / DeepSeek Flash | GPT-5 mini / Qwen Turbo | 否 |
| 转写后清洗 | 去噪、合并、断句 | Gemini Flash | DeepSeek Flash / GPT-5 mini | 否 |
| 内容理解 | 提炼核心观点 | Gemini Pro | Claude Sonnet / GPT-5.4 | 是 |
| 选题评分 | 判断国内价值 | GPT-5.4 / Claude Sonnet | Gemini Pro / DeepSeek Pro | 是 |
| 风险检测 | 版权、事实、敏感风险 | GPT-5.4 / Claude Sonnet | Gemini Pro | 是 |
| 中文重写 | 短视频脚本 | Claude Sonnet | GPT-5.4 / Gemini Pro | 是 |
| SEO 长文 | 结构化长文 | GPT-5.4 / Claude Sonnet | Gemini Pro | 是 |
| 标题生成 | 标题、简介、标签 | DeepSeek Pro / GPT-5 mini | Gemini Flash / Qwen Plus | 否 |
| JSON 结构化 | 输出稳定数据 | GPT-5.4 / GPT-5.2 | Claude Sonnet / Gemini Pro | 中 |
| 代码生成 | 系统开发 | GPT-5.2 / Codex | Claude Opus / Claude Sonnet | 是 |
| 终审 | 发布前质量判断 | GPT-5.4 / Claude Sonnet | Gemini Pro | 是 |

---

## 4. 推荐模型组合

### 4.1 稳定质量优先版

适合前期验证，少踩坑。

```text
ASR：OpenAI Whisper API
内容理解：Gemini Pro
中文脚本重写：Claude Sonnet
风险检测：GPT-5.4
结构化输出：GPT-5.4
标题/简介/标签：DeepSeek Flash
TTS：Edge-TTS / 火山引擎 TTS / Azure TTS
代码开发：GPT-5.2 / Codex
```

#### 为什么这样配

```text
Gemini：适合长文本、成本低、吃大上下文
Claude：中文口播稿更自然，改写能力强
GPT：结构化输出、风控、工具调用更稳
DeepSeek：便宜，适合批量标题和轻任务
```

---

### 4.2 成本优先版

适合跑大量内容。

```text
ASR：faster-whisper 本地
内容清洗：DeepSeek Flash
内容理解：Gemini Flash / DeepSeek Pro
中文重写：DeepSeek Pro / Qwen Max
风险检测：GPT-5 mini / Gemini Flash
标题标签：DeepSeek Flash
TTS：Edge-TTS
视频合成：ffmpeg
```

#### 问题

便宜，但质量波动更大，需要规则、重试、审核模型补上。

---

### 4.3 高质量账号版

适合后面做品牌号，不追求垃圾量。

```text
ASR：OpenAI Whisper API
内容理解：Gemini Pro
脚本重构：Claude Sonnet
二次润色：GPT-5.4
风险审查：GPT-5.4
标题封面：Claude Sonnet + GPT-5 mini A/B
TTS：Azure Neural Voice / 火山引擎 TTS
视频：自制模板 + ffmpeg
```

适合方向：

```text
AI 工具拆解
独立开发案例
海外赚钱项目
商业模式解读
技术教程中文化
SEO 长文内容库
```

---

## 5. 每条自动链的详细设计

### 5.1 发现链：自动找视频

#### 输入

```text
频道白名单
关键词
竞品账号
YouTube 搜索结果
RSS
```

#### 处理逻辑

```text
抓取新视频
→ 去重
→ 过滤时长
→ 过滤语言
→ 过滤低价值频道
→ 存入 discovered 表
```

#### 规则建议

```text
时长：3 - 45 分钟
语言：英文优先
发布时间：最近 30 天优先
播放量：小频道看增长率，大频道看主题
频道类型：商业、AI、独立开发、技术、工具、创业
```

#### LLM

```text
DeepSeek Flash / Gemini Flash
```

#### Prompt 目标

只问一个问题：

```text
这个视频是否值得进入内容分析队列？
```

---

### 5.2 理解链：自动读懂视频

#### 输入

```text
transcript.json
meta.json
comments.json 可选
```

#### 输出

```json
{
  "core_topic": "独立开发者如何用 AI 做产品",
  "main_points": [],
  "interesting_angles": [],
  "commercial_value": 8,
  "domestic_gap": 7,
  "content_formats": ["short_video", "article"],
  "risks": []
}
```

#### 推荐 LLM

```text
Gemini Pro
```

#### 原因

长上下文成本低，适合吃完整转写。

---

### 5.3 评分链：自动判断值不值得做

#### 评分公式

```text
总分 = 
国内稀缺度 × 0.25
+ 商业价值 × 0.25
+ 内容新鲜度 × 0.15
+ 争议/传播性 × 0.15
+ 可操作性 × 0.10
+ 风险反向分 × 0.10
```

#### 分数策略

| 分数 | 操作 |
|---|---|
| 85+ | 优先生成视频 + 文章 |
| 70-85 | 生成草稿，进入候选 |
| 50-70 | 只生成摘要 |
| 50 以下 | 丢弃 |

#### 推荐 LLM

```text
GPT-5.4 / Claude Sonnet
```

#### 原因

这个环节决定资源投向，不能太省。

---

### 5.4 风险链：版权和内容安全检测

#### 检测内容

```text
是否像逐字翻译
是否保留太多原作者表达
是否涉及医疗/法律/金融建议
是否涉及政治敏感
是否有虚假事实风险
是否存在未授权使用画面风险
是否适合国内平台
```

#### 输出

```json
{
  "risk_level": "medium",
  "copyright_risk": 7,
  "factual_risk": 5,
  "platform_risk": 4,
  "must_review": true,
  "suggestion": "不要使用原视频画面，改成观点拆解。"
}
```

#### 推荐 LLM

```text
GPT-5.4
Claude Sonnet
```

这里不建议用便宜模型糊弄。这个模块是防止账号出事的。

---

### 5.5 重写链：自动生成中文脚本

#### 不要这样写

```text
请翻译这个视频
```

#### 应该这样写

```text
你是中文短视频编导。
请根据原始转写内容，重构一篇适合中文观众观看的口播稿。

要求：
1. 不逐字翻译。
2. 保留核心观点，但表达必须重新组织。
3. 开头 5 秒要有强钩子。
4. 不要出现“原视频说”“这个博主说”这类搬运感表达。
5. 用中文互联网用户听得懂的话讲。
6. 增加必要背景解释。
7. 删除废话、重复、自嗨内容。
8. 如果有事实、数据、结论无法确认，标记为【待核查】。
9. 输出：
   - 标题
   - 口播稿
   - 分镜建议
   - 屏幕文字
   - 风险点
```

#### 推荐 LLM

| 任务 | 模型 |
|---|---|
| 短视频口播 | Claude Sonnet |
| 更稳结构化 | GPT-5.4 |
| 长文改写 | GPT-5.4 / Claude Sonnet |
| 批量低成本 | DeepSeek Pro / Qwen Max |

#### 主推链路

```text
Claude Sonnet 写第一稿
GPT-5.4 做结构检查和风险检查
```

---

### 5.6 多平台内容生成链

同一个原始内容，自动生成多种资产。

#### 输出结构

```text
output/video_id/
├── short_video/
│   ├── script.md
│   ├── title_options.txt
│   ├── cover_text.txt
│   └── tags.txt
├── article/
│   ├── seo_article.md
│   ├── keywords.txt
│   └── meta_description.txt
├── xiaohongshu/
│   ├── note.md
│   └── title_options.txt
├── bilibili/
│   ├── title.txt
│   ├── intro.txt
│   └── tags.txt
└── douyin/
    ├── title.txt
    ├── caption.txt
    └── hashtags.txt
```

#### 推荐 LLM

```text
GPT-5.4：SEO 文章
Claude Sonnet：小红书/短视频文案
DeepSeek Flash：标题标签批量生成
Qwen Max：中文平台风格改写
```

---

### 5.7 TTS 链：自动配音

#### 方案选择

| 方案 | 优点 | 缺点 |
|---|---|---|
| Edge-TTS | 免费/便宜，容易接入 | 声音有机器感 |
| Azure TTS | 稳定，声音质量不错 | 成本高一点 |
| 火山引擎 TTS | 中文自然度较好 | 需要接国内服务 |
| ElevenLabs | 英文强，中文看声音 | 成本较高 |
| OpenAI TTS | 接入方便 | 成本需控制 |

#### 第一版建议

```text
Edge-TTS 先跑通
```

#### 后期建议

如果做中文商业号：

```text
火山引擎 TTS / Azure Neural Voice
```

---

### 5.8 字幕链

#### 目标

自动生成：

```text
subtitles.srt
subtitles.ass
burned_video.mp4
```

#### 工具

```text
ffmpeg
pysrt
aeneas 可选
Whisper timestamps
```

#### 字幕策略

```text
每句 8-18 个中文字
每屏最多 2 行
不要一大段
关键字高亮可后期再做
```

---

### 5.9 视频合成链

#### 第一版视频样式

```text
背景视频 / 背景图
+ 标题
+ 中文配音
+ 中文字幕
+ 简单进度条
```

#### 第二版视频样式

```text
自动插入关键词卡片
自动插入截图
自动插入图表
自动切换背景
自动加 BGM
自动生成封面
```

#### 工具

```text
ffmpeg
moviepy
Pillow
Playwright 截图
```

#### 不建议第一版做

```text
复杂运镜
AI 数字人
自动混剪原视频
大量特效
```

复杂度高，但不一定提高验证效率。

---

## 6. 数据库设计

第一版用 SQLite 就够。后面再 PostgreSQL。

### 6.1 videos 表

```sql
CREATE TABLE videos (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  video_id TEXT UNIQUE,
  source_url TEXT,
  title TEXT,
  channel TEXT,
  published_at TEXT,
  duration INTEGER,
  language TEXT,
  view_count INTEGER,
  status TEXT,
  score INTEGER,
  risk_level TEXT,
  created_at TEXT,
  updated_at TEXT
);
```

### 6.2 tasks 表

```sql
CREATE TABLE tasks (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  video_id TEXT,
  task_type TEXT,
  status TEXT,
  retry_count INTEGER DEFAULT 0,
  error_message TEXT,
  created_at TEXT,
  updated_at TEXT
);
```

### 6.3 outputs 表

```sql
CREATE TABLE outputs (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  video_id TEXT,
  output_type TEXT,
  file_path TEXT,
  model_used TEXT,
  version TEXT,
  created_at TEXT
);
```

---

## 7. 项目目录

```text
yt_auto_factory/
├── app/
│   ├── main.py
│   ├── config.py
│   ├── db.py
│   ├── logger.py
│   ├── source_monitor.py
│   ├── downloader.py
│   ├── transcriber.py
│   ├── cleaner.py
│   ├── analyzer.py
│   ├── scorer.py
│   ├── risk_checker.py
│   ├── rewriter.py
│   ├── platform_writer.py
│   ├── tts.py
│   ├── subtitle.py
│   ├── renderer.py
│   ├── quality_checker.py
│   └── publisher.py
├── prompts/
│   ├── analyze_video.md
│   ├── score_topic.md
│   ├── risk_check.md
│   ├── rewrite_short_video.md
│   ├── rewrite_article.md
│   ├── title_generator.md
│   └── final_review.md
├── data/
│   ├── sources.yaml
│   └── keywords.yaml
├── workspace/
├── output/
├── logs/
├── tests/
├── requirements.txt
├── .env
└── README.md
```

---

## 8. 配置文件设计

### 8.1 `.env`

```env
OPENAI_API_KEY=xxx
ANTHROPIC_API_KEY=xxx
GEMINI_API_KEY=xxx
DEEPSEEK_API_KEY=xxx
QWEN_API_KEY=xxx

DEFAULT_ASR_PROVIDER=openai
DEFAULT_ANALYZE_MODEL=gemini-pro
DEFAULT_REWRITE_MODEL=claude-sonnet
DEFAULT_RISK_MODEL=openai-gpt
DEFAULT_TITLE_MODEL=deepseek-flash

OUTPUT_DIR=output
WORKSPACE_DIR=workspace
```

### 8.2 `sources.yaml`

```yaml
youtube_channels:
  - name: "indie hackers"
    url: "https://www.youtube.com/@xxx"
    category: "indie_hacker"
  - name: "ai tools"
    url: "https://www.youtube.com/@xxx"
    category: "ai_tools"

keywords:
  - "AI agent"
  - "indie hacker"
  - "micro SaaS"
  - "SEO case study"
  - "automation business"
```

---

## 9. 自动化阶段规划

### 阶段 1：单链接全自动生成草稿

#### 输入

```bash
python app/main.py --url "https://youtube.com/watch?v=xxx"
```

#### 输出

```text
output/video_id/
├── meta.json
├── transcript.json
├── analysis.json
├── score.json
├── risk_report.json
├── short_video_script.md
├── voice.mp3
├── subtitles.srt
├── final_video.mp4
├── titles.txt
└── description.txt
```

#### 目标

先让一个链接完整跑通。

---

### 阶段 2：批量链接处理

#### 输入

```text
data/tasks.json
```

#### 输出

```text
批量生成 output
失败自动重试
状态写入数据库
```

---

### 阶段 3：频道监控

#### 功能

```text
每天抓取频道新视频
自动去重
自动初筛
高分视频自动进入生产
低分视频只存摘要
```

---

### 阶段 4：多平台内容生产

#### 自动生成

```text
抖音口播稿
B站简介
小红书笔记
公众号长文
SEO 文章
推特/X 摘要
Telegram 推送
```

---

### 阶段 5：自动发布

最后再做。

#### 发布模式

```text
mode = "manual_review"   # 默认
mode = "auto_publish"    # 后期打开
```

#### 自动发布前置条件

```text
风险分 < 3
原创重构分 > 8
质量检测通过
字幕检测通过
视频时长符合平台要求
标题无违规词
人工抽检连续 100 条无严重问题
```

---

## 10. 质量检测链

这个模块必须做。

### 10.1 检测项

```text
视频是否生成成功
音频是否为空
字幕是否为空
字幕是否严重错位
脚本是否太像翻译
是否出现“原视频说”
是否有未核查事实
是否有敏感词
标题是否夸张违规
```

### 10.2 推荐 LLM

```text
GPT-5.4 / Claude Sonnet
```

### 10.3 输出

```json
{
  "pass": false,
  "issues": [
    "脚本中有明显翻译腔",
    "第 3 段涉及收入数据，需要核查",
    "标题过度承诺"
  ],
  "fix_suggestions": []
}
```

---

## 11. Prompt 体系

### 11.1 视频理解 Prompt

```text
你是一个中文内容选题分析师。

请阅读下面的视频转写内容，输出结构化分析。

你需要判断：
1. 这个视频的核心主题是什么
2. 它对中文用户有什么价值
3. 是否适合做短视频
4. 是否适合做 SEO 文章
5. 有哪些可传播的观点
6. 有哪些事实需要核查
7. 有哪些版权或平台风险

请输出 JSON：
{
  "core_topic": "",
  "summary": "",
  "main_points": [],
  "interesting_angles": [],
  "domestic_value": 0,
  "commercial_value": 0,
  "short_video_suitability": 0,
  "seo_suitability": 0,
  "risk_points": [],
  "recommended_formats": []
}
```

---

### 11.2 选题评分 Prompt

```text
你是一个内容商业化评估模型。

请根据视频分析结果，为这个选题打分。

评分维度：
1. 国内稀缺度 0-10
2. 商业价值 0-10
3. 传播性 0-10
4. 实操性 0-10
5. 长尾搜索价值 0-10
6. 风险等级 0-10，分数越高风险越大

请输出 JSON：
{
  "total_score": 0,
  "domestic_gap": 0,
  "commercial_value": 0,
  "virality": 0,
  "practicality": 0,
  "seo_value": 0,
  "risk_score": 0,
  "decision": "process / archive / discard",
  "reason": ""
}
```

---

### 11.3 中文短视频重写 Prompt

```text
你是一个中文短视频编导。

请基于下面的视频转写内容，重构一篇适合中文观众的短视频口播稿。

要求：
1. 不要逐字翻译。
2. 不要保留原作者的表达结构。
3. 必须重新组织逻辑。
4. 开头 5 秒要有强钩子。
5. 每句话适合口播。
6. 删除废话、重复、寒暄。
7. 不要出现“原视频说”“这个博主说”。
8. 如果涉及数据、收入、医学、金融、法律，请标记【待核查】。
9. 输出分镜建议。

输出：
# 标题
# 口播稿
# 分镜建议
# 屏幕文字
# 风险点
```

---

### 11.4 风险检测 Prompt

```text
你是内容风控审核员。

请检查下面的中文脚本是否存在风险：

1. 是否像搬运或逐字翻译
2. 是否侵犯原作者表达
3. 是否有未经核查的数据
4. 是否有医疗/法律/金融建议风险
5. 是否有平台敏感表达
6. 是否有夸大收益
7. 是否适合发布到中文平台

输出 JSON：
{
  "pass": true,
  "risk_level": "low / medium / high",
  "copyright_risk": 0,
  "factual_risk": 0,
  "platform_risk": 0,
  "issues": [],
  "must_fix": [],
  "suggested_revision": ""
}
```

---

## 12. 失败重试机制

系统一定会失败。不是可能，是一定。

### 12.1 常见失败

```text
YouTube 下载失败
字幕不存在
ASR 超时
LLM 输出不是 JSON
TTS 中断
ffmpeg 合成失败
字幕错位
内容被模型拒绝
```

### 12.2 处理策略

```text
每个任务最多重试 3 次
LLM JSON 失败自动要求修复
TTS 分段生成
长音频切片转写
每一步落盘
失败保留中间文件
```

### 12.3 JSON 修复链

推荐模型：

```text
GPT-5 mini / GPT-5.4
```

Prompt：

```text
下面内容应该是 JSON，但格式错误。
请只输出修复后的合法 JSON，不要解释。
```

---

## 13. 成本控制

### 13.1 成本原则

```text
便宜模型处理大批量
贵模型只处理高分内容
能本地化的本地化
中间结果缓存
同一个视频不要重复分析
```

### 13.2 模型调用策略

```text
所有视频：
标题初筛 → 便宜模型

分数 > 60：
完整转写 + 分析 → 中等模型

分数 > 75：
脚本重写 → 强模型

分数 > 85：
多平台内容 + 视频生产 → 强模型

发布前：
风险审核 → 强模型
```

---

## 14. 第一版 MVP

### 14.1 第一版目标

```text
输入 YouTube URL
→ 自动下载音频
→ 自动转写
→ 自动分析
→ 自动评分
→ 自动重写中文短视频脚本
→ 自动配音
→ 自动生成字幕
→ 自动合成视频
→ 输出 final_video.mp4
```

### 14.2 第一版模型配置

```text
ASR：OpenAI Whisper API
分析：Gemini Pro
评分：GPT-5.4
重写：Claude Sonnet
风险检测：GPT-5.4
标题标签：DeepSeek Flash
TTS：Edge-TTS
视频合成：ffmpeg
```

这套配置不是最省钱，但最适合先跑通。

---

## 15. 开发顺序

### Day 1：下载 + 转写

```text
downloader.py
transcriber.py
```

验收：

```text
5 个 YouTube 链接能转成 transcript.json
```

---

### Day 2：分析 + 评分

```text
analyzer.py
scorer.py
```

验收：

```text
每个视频生成 analysis.json 和 score.json
```

---

### Day 3：脚本重写 + 风险检测

```text
rewriter.py
risk_checker.py
```

验收：

```text
生成一个能读的中文口播稿
能标记风险点
```

---

### Day 4：TTS + 字幕

```text
tts.py
subtitle.py
```

验收：

```text
生成 voice.mp3 和 subtitles.srt
```

---

### Day 5：视频合成

```text
renderer.py
```

验收：

```text
生成 final_video.mp4
```

---

### Day 6：批量任务

```text
task_runner.py
db.py
```

验收：

```text
10 个链接自动跑完
失败任务能重试
```

---

### Day 7：频道监控

```text
source_monitor.py
```

验收：

```text
每天自动发现新视频
高分自动进入队列
```

---

## 16. 最小命令行设计

### 单链接

```bash
python app/main.py --url "https://youtube.com/watch?v=xxx" --mode draft
```

### 批量

```bash
python app/main.py --task-file data/tasks.json --mode batch
```

### 频道监控

```bash
python app/source_monitor.py --run-once
```

### 定时任务

```bash
0 */6 * * * /usr/bin/python3 /var/www/yt_auto_factory/app/source_monitor.py --run-once
```

---

## 17. 最终生产模式

### 17.1 推荐默认模式

```text
自动生产
人工审核
手动发布
```

### 17.2 后期高级模式

```text
低风险内容：自动发布
中风险内容：人工审核
高风险内容：丢弃
```

---

## 18. 第一版必须做的文件

```text
downloader.py
transcriber.py
analyzer.py
rewriter.py
tts.py
renderer.py
main.py
```

第一条完整自动链：

```text
Whisper API → 转写
Gemini Pro → 理解
Claude Sonnet → 中文重写
GPT-5.4 → 风险审核
DeepSeek Flash → 标题标签
Edge-TTS → 配音
ffmpeg → 合成
```

---

## 19. 最关键的产品判断

项目能不能赚钱，不取决于能不能把视频自动合出来。

取决于三个东西：

```text
1. 能不能找到国内缺但国外多的内容源
2. 能不能把它重构成中文用户愿意看的表达
3. 能不能把内容沉淀到多个平台，而不是只发一个视频
```

最终目标：

```text
海外内容机会发现
→ 中文内容资产生产
→ 多平台分发
→ SEO / 短视频 / 图文联动
```

---

## 20. 立即执行清单

### 第一步

创建项目：

```bash
mkdir yt_auto_factory
cd yt_auto_factory

mkdir app prompts data workspace output logs tests
touch app/main.py app/config.py app/downloader.py app/transcriber.py
touch app/analyzer.py app/rewriter.py app/tts.py app/renderer.py
touch requirements.txt .env README.md
```

### 第二步

安装依赖：

```bash
pip install yt-dlp openai anthropic google-generativeai python-dotenv pydub edge-tts moviepy pysrt pyyaml
sudo apt update
sudo apt install ffmpeg -y
```

### 第三步

先跑通：

```text
YouTube 链接
→ source_audio.mp3
→ transcript.json
```

不要一开始写完整大系统。先让一条链接变成一个可用草稿。
